<!DOCTYPE html>
<html lang="en">

<head>
    <title>Edit Produk</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background: #f5f5f5;
        }

        .container-box {
            width: 70%;
            margin: 40px auto;
            border: 2px solid #6b8e23;
            padding: 20px;
            background: #fff;
        }
    </style>
</head>

<body>
    <div class="container-box">
        <h2 class="mb-4 text-center">Edit Produk</h2>

        <% if (alert && alert.message) { %>
            <div class="alert alert-<%= alert.type === 'danger' ? 'danger' : 'success' %>" role="alert">
                <%= alert.message %>
            </div>
            <% } %>

                <form action="/edit/<%= product.kode_baju %>" method="POST" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label>Kode Baju:</label>
                        <input type="text" class="form-control" value="<%= product.kode_baju %>" readonly>
                    </div>
                    <div class="mb-3">
                        <label>Nama Baju:</label>
                        <input type="text" name="nama_baju" class="form-control" value="<%= product.nama_baju %>"
                            required>
                    </div>
                    <div class="mb-3">
                        <label>Ukuran:</label>
                        <select name="ukuran_id" class="form-select" required>
                            <% sizes.forEach(size=> { %>
                                <option value="<%= size.id %>" <%=Number(size.id)===Number(product.ukuran_id)
                                    ? 'selected' : '' %>>
                                    <%= size.nama_ukuran %>
                                </option>
                                <% }) %>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label>Warna:</label>
                        <input type="text" name="warna" class="form-control" value="<%= product.warna %>" required>
                    </div>
                    <div class="mb-3">
                        <label>Stok:</label>
                        <input type="number" name="stok" class="form-control" min="0" value="<%= product.stok %>"
                            required>
                    </div>
                    <div class="mb-3">
                        <label>Harga Pokok:</label>
                        <input type="number" name="harga_pokok" class="form-control" min="0"
                            value="<%= product.harga_pokok || 0 %>" required>
                    </div>
                    <div class="mb-3">
                        <label>Harga Jual:</label>
                        <input type="number" name="harga_jual" class="form-control" min="0"
                            value="<%= product.harga_jual || 0 %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="file" class="form-label">Foto:</label>
                        <input type="file" name="file" class="form-control" id="file"
                            accept=".jpg,.jpeg,.png,.webp,image/png,image/jpeg,image/webp">
                        <small class="text-muted">Format: jpg/jpeg/png/webp, maksimal 2MB.</small>
                        <% if (product.filename) { %>
                            <div class="mt-2">
                                <img src="/uploads/<%= product.filename %>" width="80" height="80">
                            </div>
                            <% } %>
                    </div>
                    <button type="submit" class="btn btn-primary">Update</button>
                    <a href="/" class="btn btn-secondary">Kembali</a>
                </form>
    </div>
</body>

</html>